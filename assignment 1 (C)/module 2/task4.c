/*Q:Compile your hello.c file using the GCC command line (gcc hello.c -o hello) and run the
   output file to display the message in your terminal.<br><br><em><strong>Hint:</strong> On Windows, the output file will be hello.exe; on Linux/Mac, it will be hello.</em> 
A:*/
   #include<stdio.h>
int main(){
   printf("hello.exe\n");
    return 0;
}
/*gcc code: cd module\ 2/ --> ENTER -->gcc task4.c -o task -->ENTER --> ./task -->ENTER */