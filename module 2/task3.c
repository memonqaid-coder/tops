/* Q: Identify and label the header, main function, and return statement in your hello.c program using comments.

A: /* 1. HEADER SECTION */
// This includes the Standard Input Output library needed for the printf function
#include <stdio.h>

/* 2. MAIN FUNCTION */
// This is the entry point of your C program where execution begins
int main() {
    
    // Printing the requested message to the console
    printf("Hello, Instagram World!\n");

    /* 3. RETURN STATEMENT */
    // This terminates the main function and returns 0 to the OS, indicating success
    return 0;
}